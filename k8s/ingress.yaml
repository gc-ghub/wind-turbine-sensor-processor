apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: turbine-sensor-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: turbine-sensor-service
                port:
                  number: 80